# Gitleaks configuration
# Extends gitleaks' default ruleset with repository-specific allowlists.
# NOTE: allowlists here are for NON-PRODUCTION test fixtures only.
title = "gitleaks config for UNION-BANK-"

[extend]
useDefault = true

[allowlist]
description = "Corruption/failure-path test fixtures (dummy values by design)"
regexes = [
  # tests/test_integration.py malformed-token test fixtures.
  '''corrupt-key-001''',
]
